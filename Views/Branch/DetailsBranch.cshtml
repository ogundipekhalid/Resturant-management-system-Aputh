@model RDMS.Models.Dtos.BranchDto
@* <img src="~/Upload/Images/@Model.Eatery.Logo" height="100" width="100"> *@
@* @using RDMS.Models.Dtos
@model IEnumerable<BranchDto> *@

@{
    ViewData["Title"] = "Branch Details"; 
    @* var menu = "~/Views/Shared/Menu.cshtml"; *@
}
<div class="">
    <div class="row">
        </div>
        <div class="row col-md-10 pt-3">
            <div class="col-md-9">
                <span class="text-center">
                    <h3>@ViewData["Title"]</h3>
                </span>
                <div style="">
                    <input type="hidden" id="messageInput" >
                    <table class="table table-bordered">
                        <tbody>
                            <tr>
                                <th> id </th>
                                <th>@Model.Id</th>
                            </tr> 
                            <tr>
                            <tr>
                                <th>Name</th>
                                <th>@Model.Name</th>
                            </tr>
                            </tr>
                           @* @foreach (var item in Model)
                           {
                             <tr>
                                <th> id </th>
                                <th>@item.EateryId</th>
                            </tr> 
                            <tr>
                            <tr>
                                <th>Name</th>
                                <th>@item.Name</th>
                            </tr>
                            </tr>
                           } *@
                        </tbody>
                    </table>
                </div>
            </div>

            <div class="col-md-3 border" style="height: 20vh; display:flex; flex-direction: column">
                <span class="text-center"></span>
              </div>
        </div>
    </div>
</div>
