@* @model RDMS.Models.Dtos.BranchDto *@

@using RDMS.Models.Dtos
@model IEnumerable<BranchDto>

@{
    @* ViewData["Title"] = "Branch Details"; *@
    @* var menu = "~/Views/Shared/Menu.cshtml"; *@
}
<div class="">
    <div class="row">
        <div class="col-md-2 border" style="min-height: 520px; padding: 0px; margin: 0px">
        </div>
        <div class="row col-md-10 pt-3">
            <div class="col-md-9">
                <span class="text-center">
                    <h3>@ViewData["Title"]</h3>
                </span>
                <div style="">
                    <input type="hidden" id="messageInput" value='@TempData["Message"]'>
                    <table class="table table-bordered">
                        <tbody>
                            
                            
                         @foreach (var item in Model)
                        {
                            <tbody>
                            <tr>
                                <th> id </th>
                                <th>@item.Id </th>
                            </tr>
                            <tr>
                            <tr>
                                <th>Name</th>
                                <th>@item.Name</th>
                            </tr>
                            
                        </tbody>
                        } 
                        
                    </table>
                </div>
            </div>

            <div class="col-md-3 border" style="height: 20vh; display:flex; flex-direction: column">
                <span class="text-center">Branch Menu</span>
                @* <a asp-action="List" asp-controller="PurchaseItem" asp-route-id="@Model.Id">Purchase List</a>
                <a asp-action="List" asp-controller="StockItem" asp-route-id="@Model.Id">Stock List</a> *@
            </div>
        </div>
    </div>
</div>

@*
@section Scripts{
<script>
$(document).on("click", "#Remove", function(){
var name = $(this).attr("name");
var result = confirm(
"Are you sure you want to delete this branch?\n"+
"Name: "+name
);
if(result == true)
{
return true;
}
else
{
return false;
}
})


</script>
} *@