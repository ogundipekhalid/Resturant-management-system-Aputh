

    @using RDMS.Models.Dtos
@model IEnumerable<FoodDto>


@* 
<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">

<style>
    .container {
        margin-top: 20px;
        background-color: #f8f9fa;
        padding: 20px;
        border-radius: 5px;
    }
     
    h3 {
        text-align: center;
        margin-bottom: 20px;
    }
    
    .table-responsive {
        margin-bottom: 20px;
    }
    
    .btn-group {
        margin-bottom: 20px;
    }
</style> *@
@* 
<div class="container">
    <h3>@ViewData["Title"]</h3>
    <div class="table-responsive">
        <table class="table table-striped table-hover table-bordered">
            <tbody>
                <tr>
                    <td>Id</td>
                    <td>Images</td>
                    <td>Name</td>
                    <td>Price</td>
                    <td>Time</td>
                    <td>Quantity</td>
                    <td>Catigories</td>
                   
                </tr>
               
                    @foreach (var item in Model)
                    {
                        <tr>
                            <img  asp-append-version="true" src="~/Upload/Images/@item.Image"  alt="item.Data image">
                            <td>@item.Id</td>
                            @* <td>@item.Image</td> 
                            <td>@item.Name</td>
                            <td>@item.Price</td>
                            <td>@item.AvailableTime</td>
                            <td>@item.Quantity</td>
                            <td>@item.Categories</td>
                            <td>
                                  @* <td>
                                        <a class="btn-warning btn" asp-action="Get" asp-controller="Food"  asp-route-id=@item.Id style="border-radius: 5px;">View</a>
                                  </td> *@
                            @* </td>
                        </tr>
                    } *@
                
                @* else
                {
                    <tr>
                        <td colspan="5" class="text-center">No Order found</td>
                    </tr>
                } *@
            @* </tbody>
        </table>
    </div>
    <div class="btn-group">
      </div>
</div> *@

     



<link rel="stylesheet" href="~/css/foods.css" />
<link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
<link rel="stylesheet" href="~/css/font-awesome.min.css" />
<script src="~/scripts/w3.js"></script>
<div class="form-group">
    <i class="fa fa-search-minus"></i><input type="search" id="search" class="form-control" oninput="w3.filterHTML('#list', '.food', this.value)" placeholder="Search for food">
</div>
@if (@Model == null || @Model.Count() == 0)
{
    <h1>No Food yet</h1>
}
else 
{
    <div class="table-responsive">
        <table class="table" id="list">
            <caption>List of foods</caption>
            <tbody>
        </div>
    <div class="images">
        @foreach (var food in @Model)
        {
            <div class="post-image">
                 <img  asp-append-version="true" src="~/Upload/Images/@food.Image"  alt="food.Data image">
                        <h6 class="food"></h6>
                        <h6>@food.Id <input type="hidden" value="hidden"> </h6>
                        <h6>@food.Name</h6>
                        <p>@food.Price</p>
                        <p>@food.AvailableTime</p>
                        @* <p>@food.Quantity</p> *@
                        @* <p>@food.Category</p> *@
                   
            </div>
           
        }

    </div>

}
    
       
              
           

        




















